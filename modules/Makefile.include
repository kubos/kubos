
#ifdef MODULES_LOCATION
#	EXTERNAL_MODULE_DIRS += $(KUBOS_MODULES)/location
#	INCLUDES += -I$(KUBOS_MODULES)/location
#	LINKFLAGS += -lgps
#endif

#ifdef MODULES_RADIO
#	EXTERNAL_MODULE_DIRS += $(KUBOS_MODULES)/radio
#	INCLUDES += -I$(KUBOS_MODULES)/radio
#endif

#ifdef MODULES_GPS
#	EXTERNAL_MODULE_DIRS += $(KUBOS_MODULES)/gps
#	CFLAGS += -I$(KUBOS_MODULES)/gps
#endif

ifeq ($(BOARDTYPE),beaglebone)
	CFLAGS += -DHAVE_NATIVE_PERIPH_CONF_H
	LINKFLAGS += -lrt
endif
