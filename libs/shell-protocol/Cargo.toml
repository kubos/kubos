[package]
name = "shell-protocol"
version = "0.1.0"
authors = ["Ryan Plauche <ryan@kubos.co>"]

[dependencies]
cbor-protocol = { path = "../cbor-protocol" }
channel-protocol = { path = "../channel-protocol" }
failure = "0.1.2"
log = "^0.4.0"
rand = "0.5"
serde_cbor = "0.8"
simplelog = "^0.5.0"