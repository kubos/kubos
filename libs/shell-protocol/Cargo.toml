[package]
name = "shell-protocol"
version = "0.1.0"
authors = ["Ryan Plauche <ryan@kubos.co>"]

[dependencies]
simplelog = "^0.5.0"
serde_cbor = "0.8"
log = "^0.4.0"
cbor-protocol = { path = "../cbor-protocol" }
failure = "0.1.2"
rand = "0.5"