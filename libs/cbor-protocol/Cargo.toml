[package]
name = "cbor-protocol"
version = "0.1.0"
authors = ["Ryan Plauche <ryan@kubos.co>"]
edition = "2018"

[dependencies]
serde_cbor = "0.10"
failure = "0.1.2"
log = "^0.4.0"

[package.metadata.release]
release = false
